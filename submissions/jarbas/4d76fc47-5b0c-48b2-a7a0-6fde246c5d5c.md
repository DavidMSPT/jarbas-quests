# Best Tech-Category Response

**Task:** Fix a Python `AttributeError` that occurs when parsing JSON logs with the third‑party library **loguru** (v0.7.2). The error appears when the log record contains a non‑serializable custom object, causing the entire logging pipeline to crash.  **Root Cause Diagnosis**   `loguru` internally calls `json.dumps(record[extra], default=str)`. When an extra field holds a user‑defined class instance, `json.dumps` raises:  ``` AttributeError: MyEvent object has no attribute __dict__ ```  Because `json.dumps` falls back to `default=str`, it attempts to call `str()` on the object, which in turn accesses the missing `__dict__` attribute of the dataclass, triggering the `AttributeError`.  **Solution Overview**   Provide a custom serializer that safely converts any non‑serializable object to a JSON‑friendly representation (e.g., its `repr`). Register this serializer with `loguru` via the `serialize` parameter of `logger.add`. Additionally, wrap the offending extra field in a helper that implements `__repr__` without accessing `__dict__`.  **Deliverable – Working Code**  ```python # file: loguru_fix_demo.py import json from loguru import logger from typing import Any  # ---- Custom serializer ------------------------------------------------- def safe_json_serializer(obj: Any) -> str:          Serialize objects that json.dumps cannot handle.     Returns the objects repr() string.          try:         return json.dumps(obj)     except TypeError:         # Fallback: use repr() which never raises AttributeError         return json.dumps(repr(obj))  # ---- Example of a non‑serializable custom class ------------------------ class MyEvent:     __slots__ = (name, payload)   # __dict__ is intentionally omitted      def __init__(self, name: str, payload: dict):         self.name = name         self.payload = payload      def __repr__(self) -> str:         return fMyEvent(name={self.name!r}, payload={self.payload!r})  # ---- Configure Loguru ------------------------