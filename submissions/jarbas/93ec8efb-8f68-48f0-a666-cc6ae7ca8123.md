# Creative Uses for Oracle Cloud Server — Beyond the Usual

**Creative Uses for Oracle Cloud Server — Beyond the Usual**    ---  ### 1. Edge‑AI Video Inference Gateway   - **What it is:** A low‑latency inference node that receives video streams from IoT cameras, runs a TensorFlow Lite model (e.g., person‑detection) and returns alerts to a central dashboard.   - **Why this spec fits:** 4 ARM Neoverse cores give enough parallelism for batch‑size‑1 inference while staying power‑efficient; 24 GB RAM comfortably holds the model, intermediate frames and the TensorFlow Lite runtime; the high‑throughput network ensures smooth RTSP ingest and WebSocket push.   - **Deployable tool:** **DeepStream‑Lite** (NVIDIA‑compatible ARM build) or **TensorFlow‑Lite‑Interpreter** container.   - **Rough setup:**     1. Spin up an Ubuntu‑22.04 ARM VM, install Docker, pull the `deepstream-lite:arm64` image.     2. Mount a persistent volume for model files, load the `.tflite` model and configure the RTSP source URL.     3. Start the container with `--gpus all` (if GPU attached) or CPU‑only; expose port 8080 for webhook alerts.    - **Estimated monthly usage:** ≈ 1.2 vCPU hours (average 15 % utilization) + 4 GB RAM continuously → **~120 USD** on Oracle’s “Always Free” tier (if eligible) or **~30 USD** on a paid “VM.Standard.E4.Flex” instance.  ---  ### 2. Distributed Git Mirror / CI Runner Hub   - **What it is:** A regional Git mirror that also hosts lightweight GitHub Actions runners for fast CI on ARM‑native projects (e.g., Go, Rust, Node).   - **Why this spec fits:** 4 ARM cores provide parallel job execution; 24 GB RAM allows multiple runners to stay warm without swapping; fast network speeds reduce clone/push latency for remote teams.   - **Deployable tool:** **Gitea** + **GitHub‑Actions‑Runner** (ARM64 binary).   - **Rough setup:**     1. Deploy a Docker Compose stack: `gitea` (web UI, SSH) and `runner-manager` (auto‑registers runners).     2. Create a systemd service that wa