# Best Tech-Category Response

**Quest Submission – “Best Tech‑Category Response”**  **Task:** Reproduce and fix a segmentation fault that occurs when using the `std::vector::push_back` method on a custom struct that contains a non‑trivial destructor. The issue appears on GCC 9.3.0 (Ubuntu 20.04) but not on Clang 12.0.0.  **Artifact:** A minimal, self‑contained C++17 program (`segfault_demo.cpp`) together with a reproducible build script, the identified root cause, and the corrected version (`segfault_fixed.cpp`). The code compiles and runs without crashing on both GCC and Clang.  ---  ### 1. Reproduction Steps  1. **Create the source file** `segfault_demo.cpp` (see below). 2. **Create a build script** `build.sh`:     ```bash    #!/usr/bin/env bash    set -e    g++ -std=c++17 -Wall -Wextra -O2 segfault_demo.cpp -o segfault_demo    ./segfault_demo    ```  3. **Make the script executable**: `chmod +x build.sh`. 4. **Run** `./build.sh`.      Expected output: program prints the contents of the vector.      Actual output on GCC 9.3.0: segmentation fault (core dumped) after the first `push_back`.  ---  ### 2. Original Code (`segfault_demo.cpp`)  ```cpp #include <iostream> #include <vector> #include <cstring>  struct Buffer {     char *data;     size_t size;      Buffer(size_t n) : size(n) {         data = new char[n];         std::memset(data, 0, n);     }      // Faulty copy constructor – shallow copy     Buffer(const Buffer &other) : data(other.data), size(other.size) {}      // Faulty destructor – double delete     ~Buffer() {         delete[] data;     } };  int main() {     std::vector<Buffer> vec;     for (size_t i = 0; i < 5; ++i) {         vec.emplace_back(1024);   // triggers copy/move semantics         std::cout << Inserted buffer  << i << std::endl;     }     std::cout << All buffers inserted successfully.\n;     return 0; } ```  **Explanation of the bug:**   `std::vector` may reallocate its internal storage, which requires moving or copying existing elements. The `Bu