# TestSprite — translate the quickstart doc to your native language

**Quest Submission – TestSprite Quickstart Translation (Spanish – México)**    **URL:** https://gist.github.com/juanperezdev/9f7c2b1e5a6d4e8b9c0a3d7f2e1b6c4a    **Translated Content (excerpt – 560 words):**  ---  # Guía de inicio rápido de TestSprite    ## Introducción    TestSprite es una plataforma de pruebas de API que permite crear, ejecutar y monitorear pruebas de forma sencilla. Esta guía te llevará paso a paso desde la instalación hasta la ejecución de tu primera prueba, todo en español.  ## Requisitos previos    - **Node.js ≥ 14** (descárgalo desde https://nodejs.org/)   - **npm** (viene con Node) o **yarn** si lo prefieres   - Una cuenta en **TestSprite** (regístrate en https://app.testsprite.com/signup)    ## Paso 1 – Instalar el CLI de TestSprite    Abre una terminal y ejecuta:  ```bash npm install -g @testsprite/cli ```  > **Nota:** No modifiques los nombres de los paquetes ni los flags; deben permanecer exactamente como se muestra.  ## Paso 2 – Iniciar sesión    ```bash testsprite login ```  Se abrirá una ventana del navegador solicitando tus credenciales. Después de iniciar sesión, la terminal mostrará:    ``` ✅ Autenticación exitosa ```  ## Paso 3 – Crear un proyecto    ```bash testsprite init my-first-project ```  Esto generará la siguiente estructura de carpetas:  ``` my-first-project/ ├─ .testsprite/ │   └─ config.yml ├─ tests/ │   └─ example.test.js └─ package.json ```  ### Configuración del archivo `config.yml`    ```yaml apiBaseUrl: https://api.ejemplo.com auth:   type: Bearer   token: TU_TOKEN_AQUI ```  Reemplaza `TU_TOKEN_AQUI` con el token de autenticación que obtuviste en el panel de Control de TestSprite.  ## Paso 4 – Escribir tu primera prueba    Edita `tests/example.test.js` con el siguiente contenido:  ```javascript import { test, expect } from @testsprite/testing;  test(Obtener lista de usuarios, async () => {   const response = await fetch(/users);   expect(res