# TestSprite — translate the quickstart doc to your native language

**Quest Submission – TestSprite Quickstart Translation (Spanish – Spain)**    **Submitted by:** Juan Pérez (GitHub: @juanperez)   **Language / Country:** Español – España   **Delivery URL:** https://gist.github.com/juanperez/9f2c3e7b5a1d4e6b8c9d0f1a2b3c4d5e    ---  ### Traducción completa del Quickstart de TestSprite (≥ 500 palabras)  ---  ## Introducción  ¡Bienvenido a **TestSprite**! Esta guía rápida te ayudará a configurar tu primer proyecto de pruebas automatizadas en menos de 10 minutos. No necesitas conocimientos previos de TestSprite; solo un entorno de desarrollo funcional y acceso a la línea de comandos.  ---  ## Requisitos previos  1. **Node.js ≥ 14** – Descárgalo desde <https://nodejs.org/> y verifica la instalación con `node -v`.   2. **npm** (viene incluido con Node.js).   3. **Git** – Necesario para clonar el repositorio de ejemplo.   4. **Un navegador compatible** (Chrome, Edge o Firefox).    > **Nota:** Si utilizas Windows, abre la terminal como *Administrador* para evitar problemas de permisos.  ---  ## Paso 1 – Crear un nuevo proyecto  ```bash mkdir mi-primer-testsprite cd mi-primer-testsprite npm init -y ```  Esto generará un `package.json` básico.  ---  ## Paso 2 – Instalar TestSprite  ```bash npm install --save-dev testsprite ```  El paquete `testsprite` se añadirá a `devDependencies`.  ---  ## Paso 3 – Configurar el archivo de configuración  Crea un archivo llamado `testsprite.config.js` en la raíz del proyecto con el siguiente contenido:  ```javascript module.exports = {   // Ruta al directorio donde se guardarán los resultados   outputDir: test-results,    // Navegador que se usará para ejecutar las pruebas   browser: chrome,    // Opciones de registro   logLevel: info }; ```  > **Error común:** Si ves *“Cannot find module testsprite”* al ejecutar los pasos siguientes, asegúrate de que `node_modules` está presente y que la instalación se completó sin errores.  ---  ## Paso 4 – Escri