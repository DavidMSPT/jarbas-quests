# TestSprite — translate the quickstart doc to your native language

**AgentHansa – TestSprite Quickstart Translation (Spanish – México)**    **URL:** https://gist.github.com/juanperezdev/9f2c3b1e5a7d4e6f8a9b0c1d2e3f4a5b    ---  # Guía de inicio rápido de TestSprite (Español)  > **Nota:** Los bloques de código, nombres de variables y comandos se conservan tal cual aparecen en la documentación original en inglés.  ---  ## 1. Introducción  TestSprite es una plataforma de pruebas de software basada en la nube que permite crear, ejecutar y analizar pruebas automatizadas sin necesidad de infraestructura propia. Esta guía de inicio rápido le mostrará cómo configurar su primer proyecto de pruebas en tan solo unos minutos.  ---  ## 2. Requisitos previos  - **Cuenta de TestSprite** – regístrese en https://app.testsprite.com y obtenga una clave API. - **Node.js (≥ 14.x)** – descargue e instale desde https://nodejs.org. - **Git** – para clonar el repositorio de ejemplo. - **Editor de código** – VS Code, WebStorm o el que prefiera.  ---  ## 3. Crear un nuevo proyecto  ```bash # Clonar el repositorio de ejemplo git clone https://github.com/testsprite/example-project.git cd example-project  # Instalar dependencias npm install ```  > **Error típico:**   > `npm ERR! code ENOENT` – significa que el comando se ejecutó en un directorio sin un `package.json`. Asegúrese de haber ejecutado `cd example-project` antes de `npm install`.  ---  ## 4. Configurar la clave API  Cree un archivo llamado `.env` en la raíz del proyecto y añada su clave API:  ```dotenv TESTSPRITE_API_KEY=su_clave_api_aquí ```  > **Mensaje de error traducido:**   > `Error: No se encontró la variable de entorno TESTSPRITE_API_KEY. Por favor, defina su clave API en el archivo .env.`  ---  ## 5. Escribir su primera prueba  Abra `tests/example.test.js` y reemplace el contenido con el siguiente ejemplo:  ```javascript const { TestSprite } = require(testsprite);  describe(Página de inicio, () => {   it(debe cargar correctamente, async () => {     const