# Best Tech-Category Personal Task

**Task Title:** Integrating Stripe Payments with a Next.js E-commerce Application (TypeScript)  **Task Description:**   I am building a small e‑commerce site using Next.js 14 with TypeScript and Tailwind CSS. The front‑end displays product listings fetched from a headless CMS (Contentful) and the shopping cart is managed with React Context. I need to add a secure, PCI‑compliant checkout flow using Stripe’s latest Payment Intent API.  Specific requirements:  1. **Server‑Side Integration**      - Create a Next.js API route (`/api/checkout/create-payment-intent`) that receives the cart items, calculates the total amount (including tax and optional discount codes), and creates a Stripe PaymentIntent.      - The route must validate the incoming request body against a Zod schema to ensure correct data types (product IDs, quantities, currency, etc.).      - Return the client secret to the front‑end in a JSON response.  2. **Client‑Side Checkout**      - Use `@stripe/stripe-js` and `react-stripe-js` to load Stripe.js with the publishable key.      - Implement a `CheckoutForm` component that mounts the `Elements` provider, collects the card details via `CardElement`, and confirms the payment using `stripe.confirmCardPayment`.      - Handle success, error, and loading states, displaying appropriate UI feedback.  3. **Webhooks**      - Set up a webhook endpoint (`/api/webhooks/stripe`) to listen for `payment_intent.succeeded` events.      - Verify the webhook signature using the Stripe secret and update the order status in a PostgreSQL database (via Prisma).      - Ensure idempotent handling to avoid duplicate order creation.  4. **Testing & Deployment**      - Write unit tests for the API route using Jest and Supertest, mocking Stripe’s SDK.      - Provide a script to run a local Stripe CLI webhook listener for development.      - Document environment variables needed for both development and production (e.g., `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEX