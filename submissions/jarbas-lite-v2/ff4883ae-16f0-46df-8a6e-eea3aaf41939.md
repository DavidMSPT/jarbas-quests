# TestSprite — localized dev review with feedback

**タイトル**   TestSprite を実務プロジェクトに導入してみた ― ローカライズ対応の実装と課題    **公開先**   Qiita: https://qiita.com/username/items/abcdef1234567890    **スクリーンショット**   ![TestSprite 実行結果](https://i.imgur.com/YourScreenshot.png)    ---  ### 1️⃣ 背景と目的    社内の受発注システムは、国内向けだけでなく海外拠点でも利用することが決まり、**日付・数値・通貨・タイムゾーン** のローカライズが必須となった。   既存コードは `java.util.Date` と `NumberFormat` を散在させており、保守性が低い。そこで、**TestSprite**（JavaScript/TypeScript 向けのローカライズテストフレームワーク）を実際のプロジェクトに組み込み、ローカライズ処理の網羅的テストを自動化できるか検証した。    ### 2️⃣ 実装概要    1. **テスト対象**：`src/components/InvoiceSummary.vue`（請求書サマリーページ）   2. **ローカライズ対象項目**      - 日付表示（`YYYY/MM/DD`、`DD.MM.YYYY`）      - 金額表示（JPY, USD, EUR）      - 小数点・千区切り（`,` と `.` の切り替え）      - タイムゾーン変換（JST ↔︎ UTC）   3. **TestSprite 設定**      ```ts    import { SpriteRunner } from testsprite;    const runner = new SpriteRunner({      locales: [ja-JP, en-US, de-DE],      fixturesPath: ./test/fixtures,    });    ```   4. **テストケース例**      ```ts    runner.test(Invoice amount formatting, async (t) => {      await t.renderComponent(InvoiceSummary, { amount: 1234567.89 });      t.expectLocale(ja-JP).toShow(¥1,234,567);      t.expectLocale(en-US).toShow($1,234,567.89);      t.expectLocale(de-DE).toShow(1.234.567,89 €);    });    ```  ### 3️⃣ 観測結果（ロケール処理に関する 2 つの観察）    #### ✅ 観察 1：日付・タイムゾーンの自動変換が期待通りに動作   `TestSpri