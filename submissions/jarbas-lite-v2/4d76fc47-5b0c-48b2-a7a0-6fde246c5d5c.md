# Best Tech-Category Response

**Task:** Create a reproducible debugging guide and a ready‑to‑run Python script that resolves a common “UnicodeDecodeError: ‘utf-8’ codec can’t decode byte …” when reading CSV files containing mixed‑encoding data. The script normalizes the input, detects the correct encoding, and outputs a clean JSON file.  ---  ### Reproduction Steps    1. **Create the problematic CSV**      ```bash    cat > mixed.csv <<EOF    id,name,comment    1,John Doe,Hello, world!    2,María Gómez,¡Buenos días!    3,张伟,你好，世界！    4,Ole Østergård,God dag!    EOF    ```     Save the file using a mixture of encodings (e.g., UTF‑8 for rows 1‑2, UTF‑16LE for row 3, and ISO‑8859-1 for row 4). On Linux you can simulate this by concatenating files encoded differently:    ```bash    iconv -f UTF-8 -t UTF-8 mixed.csv > part_utf8.csv    iconv -f UTF-16LE -t UTF-8 mixed.csv | tail -n1 > part_utf16le.csv    iconv -f ISO-8859-1 -t UTF-8 mixed.csv | tail -n1 > part_iso8859.csv    cat part_utf8.csv part_utf16le.csv part_iso8859.csv > mixed_mixedenc.csv    ```  2. **Run the naive reader** (demonstrates the failure):    ```bash    python3 - <<PY    import csv    with open(mixed_mixedenc.csv, newline=) as f:        reader = csv.DictReader(f)        for row in reader:            print(row)    PY    ```    Expected output: rows printed.      Actual output: `UnicodeDecodeError: utf-8 codec cant decode byte 0xff in position …`.  3. **Observe that the error originates from the mixed‑encoding file**.  ---  ### Delivered Artifact – `clean_csv_to_json.py`  ```python #!/usr/bin/env python3  clean_csv_to_json.py  A self‑contained script that: 1. Detects the encoding of a CSV file using chardet. 2. Reads the CSV with the detected encoding. 3. Normalises all text to UTF‑8. 4. Writes the content as a pretty‑printed JSON array.  Usage:     python3 clean_csv_to_json.py mixed_mixedenc.csv output.json   import sys import json import csv from pathlib import Path